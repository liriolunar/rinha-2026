[project]
name = "fraud-api"
version = "0.1.0"
description = "Rinha de Backend 2026 — Fraud Detection API"
requires-python = ">=3.13"

dependencies = [
    "socketify>=0.0.31",
    "orjson==3.10.16",
    "numpy==2.2.5",
]
